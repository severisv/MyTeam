@model UserMember

<div class="login-image-wrapper">
    <a title="@Model.FirstName" asp-controller="player" asp-action="show" asp-route-playerid="@Model.Id">
        <img src="@Cloudinary.MemberImage(Model.Image, Model.FacebookId, 40, 40)"/>
    </a>
</div>
