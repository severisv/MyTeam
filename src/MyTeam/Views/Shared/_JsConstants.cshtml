@using MyTeam.Models.Enums

<script>
    var Routes = Routes || {};
    Routes.GET_PLAYERS = '@Html.Raw(Url.Action("GetPlayers", "Api"))';
    Routes.ADD_PLAYER = '@Html.Raw(Url.Action("AddPlayer", "Admin"))';
    Routes.GET_FACEBOOK_IDS = '@Html.Raw(Url.Action("GetFacebookIds", "Api"))';
    
    var Constants = Constants || {};
    Constants.Active = '@Html.Raw(PlayerStatus.Aktiv)';
    Constants.Inactive = '@Html.Raw(PlayerStatus.Inaktiv)';
    Constants.Veteran = '@Html.Raw(PlayerStatus.Pensjonert)';
</script>